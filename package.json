{
  "name": "workpilot-root",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix backend ci && npm --prefix backend run build",
    "start": "npm --prefix backend start"
  },
  "dependencies": {
    "mongodb": "^7.5.0"
  }
}